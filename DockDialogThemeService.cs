namespace GlueDock;


    // DockDialogThemeService moved to GlueDock.WidgetApi so Core and external widget DLLs use the same central dialog/settings styling service.

    // GlueDock UI rule: Do not define a second DockDialogThemeService in the Core project.

    