namespace GlueDock;


    // DockDialogTheme moved to GlueDock.WidgetApi so Core and external widget DLLs use the same central dialog/settings theme.

    // GlueDock UI rule: Do not define a second DockDialogTheme in the Core project.

    